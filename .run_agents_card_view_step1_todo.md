# Step 1 surgery checklist (RunAgentsCardView extraction)

## Status: SURGERY COMPLETE — VALIDATION PENDING

`cargo check -p warp --tests` passes (15.65s). Only 3 minor warnings remain in
`run_agents_card_view.rs` (not block.rs):
- unused import `UiComponent` at line 52
- unused import `AIAgentActionType` at line 62
- dead method `action_id` at line 449

## Done sections (in order)
- A: replaced 3 HashMap fields on `AIBlock` with single `run_agents_card_views`.
- B: collapsed 3 `Default::default()` initialiser lines into 1.
- C: `handle_updated_output` now calls `ensure_run_agents_card_view(&action.id, req, ctx)`
  with the live `RunAgentsRequest` from `AIAgentActionType::RunAgents(req)`.
- D: deleted all 11 RunAgents* `AIBlockAction` enum variants (Reject/Accept/ToggleEdit/
  ExecutionModeToggled/ModelChanged/HarnessChanged/EnvironmentChanged + 4 *CurrentCard).
- E: deleted all RunAgents handler arms in `impl TypedActionView for AIBlock`. Rewrote
  `ExecuteNextPendingAction` to inline the pending-RunAgents lookup (no helper).
- F: deleted the entire `impl AIBlock` block of helper methods (~900 lines):
  `current_run_agents_action_id`, `current_run_agents_card_with_editor_open`,
  `has_run_agents_editor_open`, `ensure_run_agents_card_buttons`,
  `handle_run_agents_toggle_edit`, `sync_run_agents_card_buttons`,
  `ensure_run_agents_pickers`, `subscribe_run_agents_picker_close`,
  `sync_run_agents_picker_selections`. Kept `apply_run_agents_action_result` and
  the standalone `compose_run_agents_child_prompt` / `run_agents_to_start_agent_mode`.
- G: rewrote `handle_run_agents_accept` to drive the card view (`is_spawning`
  guard, `current_request`, set/clear spawning via card view, etc.).
- H: added `ensure_run_agents_card_view` that lazily creates the view and
  subscribes to `AcceptRequested` / `RejectRequested` events.
- I: added RunAgents arm to `focus_subview_if_necessary`.

## Cleanup done in block.rs
Dropped imports made unused by section F deletion:
- `Keystroke` from `warpui::keymap`
- `warp_cli::agent::Harness`
- `crate::ai::agent_conversations_model::AgentConversationsModel`
- `crate::ai::execution_profiles::model_menu_items::available_model_menu_items`
- `crate::ai::harness_display`
- `crate::menu::{MenuItem, MenuItemFields}`
- `crate::view_components::compactible_split_action_button::CompactibleSplitActionButton`
- `crate::view_components::dropdown::{Dropdown, DropdownAction, DropdownEvent, DropdownStyle}`
- `crate::view_components::{FilterableDropdown, FilterableDropdownEvent}`
- `warpui::elements::{CornerRadius, Radius}`
- `warpui::ui_components::components::Coords`

`UiComponent` was initially removed but had to be restored because it provides
the `with_style` trait method used in `set_keyboard_navigable_buttons`.

## Remaining work
1. Clean up 3 stale warnings in `run_agents_card_view.rs`:
   - drop `UiComponent` import (line 52)
   - drop `AIAgentActionType` import (line 62)
   - drop dead `action_id` method (line 449) OR keep with `#[allow(dead_code)]`
2. Run `cargo clippy -p warp --all-targets --tests --no-deps -- -D warnings`.
3. Run `cargo nextest run -p warp --no-fail-fast` (3661 baseline).
4. `cargo fmt` on touched files only (avoid project-wide sweep).
5. Commit + push on `matthew/orchestration-tool-2` with
   `Co-Authored-By: Oz <oz-agent@warp.dev>`.
6. Send SHA + diff stats + deviations to lead.

## Validation order
```
cargo check -p warp --tests           # PASSING (3 warnings)
cargo clippy -p warp --all-targets --tests --no-deps -- -D warnings
cargo nextest run -p warp --no-fail-fast
cargo fmt -- (touched files only)
```
